# 统一换行符：仓库内一律 LF，避免 Windows 检出时把 shell 脚本写成 CRLF
# （CRLF 的 start.sh 在 Linux/macOS 上会报 bad interpreter）
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制/生成物不参与换行符转换
*.png binary
*.jpg binary
*.gz binary
